1. 图像生成的主要路径：
	1. 基于sde的路径（如score）
	2. 基于ode的flow路径
2. 条件生成的主要路径：
	1. 基于二元组的配对
		1. 基于ode的flow路径（部分）
		2. 基于sde的路径（如bridge）
	2. 基于check point的方法（基于如下事实，(x,text)远远多于(x,y,text)）
		1. 基于挂件的方法（controlnet）
		2. 基于attention的方法（flux_kontext, LDM）
		3. 啥也不练，加噪-去噪法（SDEdit）